[project]
name = "ramp-udp"
version = "1.0.0"
description = "Reliable Authenticated Messaging Protocol over UDP"
requires-python = ">=3.11"

[tool.black]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]